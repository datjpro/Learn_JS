//17: toán tử 3 ngôi
let so = 10;
let mes = so > 0 ? "Số dương" : "Số âm";
console.log(mes); // in ra "Số dương" nếu so > 0, ngược lại in ra "Số âm"

let so1 = 20;
let mes1 = so1 % 2 == 0 ? "số chẵn" : "số lẻ";
console.log(mes1); // in ra "số chẵn" nếu so1 là
